_format_version: "3.0"
# =============================================================================
# Kong AI Gateway - Route Definition for Prompt Guard Demo
# Demo: ai-prompt-guard — block PII and prompt injection attacks
# Depends on: ollama-service (aigw-service-kng.yaml)
# =============================================================================
routes:
- name: ollama-route-guard
  service:
    name: ollama-service
  paths:
  - /ai/guard/chat
  protocols:
  - http
  - https
  strip_path: true
  path_handling: v0
  https_redirect_status_code: 426
  regex_priority: 0
  request_buffering: true
  response_buffering: true
  preserve_host: false
